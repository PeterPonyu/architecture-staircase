name: Visual pixel (optional, gated)

# Optional / gated until portal/reference.png is user-approved.
# Must not fail required CI (job if: false).
on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read

jobs:
  visual-pixel:
    name: Pixel diff vs approved reference
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Visual Ralph pixel-diff is gated until reference.png approval."
